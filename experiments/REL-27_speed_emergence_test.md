# REL-27 — Numerical result

The tested relational model gives

`v_bound = Rℓ/τ`.

For `R=2`:

- `ℓ=1, τ=0.5` → `v=4`
- `ℓ=3, τ=0.5` → `v=12`
- `ℓ=1, τ=1` → `v=2`
- `ℓ=5, τ=2.5` → `v=4`

The abstract graph and reach rule are unchanged. Independent rescaling of length and time changes the numerical speed.

Result: the architecture produces a finite speed scale, but does not determine its numerical value.

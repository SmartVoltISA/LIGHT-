# LIGHT — Derivation Chain Addendum 2026-09-07

This addendum extends `DERIVATION_CHAIN.md` without rewriting its historical record.

## New spacetime branch

`DISTINCTION + RELATION + ORDER + BOUNDARY + LOCALITY`
→ `[UNRESOLVED: causal-cone / relativity principle]`
→ `LORENTZ STRUCTURE`
→ `WORLDLINE INTERVAL`
→ `DYNAMICS`

REL-21 establishes the countermodel boundary: the weak relational set also admits a Galilean construction, so Lorentzian geometry and invariant `c` are not derived from those requirements alone.

REL-22 establishes the conditional step:

`finite invariant causal cone + inertial-frame relativity + linearity/reciprocity`
→ `Lorentz transformation`
→ `Minkowski interval preservation`.

REL-20 then establishes conditionally:

`Lorentz structure + reparameterization invariance + locality`
→ `sqrt(u·u)` worldline functional
→ low-speed `v²` form.

## Current exact boundary

The deepest open question is now narrower:

`Ω relational primitives → finite invariant causal cone / relativity principle → Lorentzian geometry`

The first arrow remains **UNRESOLVED**.

The second arrow is **CONDITIONALLY SUPPORTED** by REL-22.

The metric-to-worldline-functional step is **CONDITIONALLY SUPPORTED** by REL-20.

No value of `c` or mass scale has been derived.

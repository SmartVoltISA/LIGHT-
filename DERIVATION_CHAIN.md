# LIGHT — Derivation Chain

This document records the conceptual derivation without claiming that each step is logically inevitable in all formulations.

## Ω foundation → dynamics

`WILL → MEMORY → VALUE → CHOICE → PATH`

REL-16 and REL-17 establish the negative boundary that generic VALUE does not by itself determine a physical trajectory.

The tested sufficient extension is:

`PATH → local physical functional S[P] → stationary selection δS=0 → equations of motion → CHANGE`

REL-18 then separates symmetry from the dynamical selection law:

`time-translation symmetry → conserved energy-like quantity`

The physical Lagrangian form, mass parameter, spacetime structure, and stationary-action principle remain external inputs at this stage.

## QED structural chain

1. Relativistic quantum matter is represented by fields.
2. Charged matter has a local phase symmetry U(1).
3. Making that symmetry local requires a covariant derivative.
4. The covariant derivative introduces A_mu.
5. Gauge-invariant dynamics require F_muν = ∂_mu A_ν − ∂_ν A_mu.
6. The Maxwell/QED Lagrangian determines field dynamics and coupling.
7. Quantization of the electromagnetic field yields photon excitations.
8. Photons and charged matter participate in electromagnetic processes.
9. Experiments measure invariant consequences rather than gauge-dependent potentials directly.

## Structural compression

`WILL → MEMORY → VALUE → CHOICE → PATH`
→ `LOCAL PHYSICAL FUNCTIONAL`
→ `STATIONARY SELECTION`
→ `DYNAMICS`
→ `SYMMETRY`
→ `CONSERVATION`

For QED, the externally supplied physical structure is then represented as:

`U(1) local symmetry`
→ `connection A_mu`
→ `curvature F_muν`
→ `action`
→ `quantum modes`
→ `photon`
→ `interaction`
→ `observation`

## Why this matters

The photon is not the first item in the derivation. The field-theoretic structure comes first. This makes the photon a state/excitation of an already-defined relational field structure.

The new REL-18 result also prevents a common conceptual collapse: abstract VALUE, physical ACTION, symmetry and conservation are related but are not the same object.

## Caution

This compression is a conceptual map. It does not replace the formal derivation of QED, and it does not imply that gauge symmetry is an emergent property of Ω. REL-18 is a sufficiency test inside a specified variational class, not a derivation of the physical action from Ω primitives.
